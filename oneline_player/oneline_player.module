<?php
// $Id$

function oneline_player_init() {
  drupal_add_css(drupal_get_path('module', 'oneline_player') .'/oneline_player.css', 'file');
  drupal_add_js(drupal_get_path('module', 'oneline_player') .'/oneline_player.js', 'file');
}

function oneline_player_transcripts_info() {
	return array(
		'name' => t('One-line player'),
		'to' => 'toOneline',
		'back' => 'fromOneline',
	);
}

?>
